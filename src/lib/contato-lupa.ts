/**
 * Canal de contato institucional — Instagram e e-mail (#239).
 *
 * Diferente de `CONTROLADOR` (usado em `/termos`, `/privacidade` e
 * `/suporte`), este arquivo não depende de a PALU terminar de se
 * constituir: Instagram e e-mail de contato não são documento legal, só
 * um jeito de encontrar a Lupa. As duas caixas são reais e testadas antes
 * de entrarem aqui — publicar um endereço morto seria a mesma promessa
 * quebrada que este projeto já registrou várias vezes no AGENTS.md.
 */

export const INSTAGRAM_LUPA = "https://www.instagram.com/lupapp.br/";

/** Assunto comercial, parceria, imprensa. */
export const EMAIL_CONTATO = "contato@lupapp.com.br";

/** Quem já usa o app e precisa de ajuda. */
export const EMAIL_SUPORTE = "suporte@lupapp.com.br";
